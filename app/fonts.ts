import { Heebo, Bitter } from "next/font/google";

export const heebo = Heebo({ 
    subsets: ["latin"],
    weight: ['400', '600', '800'] });

export const bitter = Bitter({ 
    subsets: ["latin"],
    weight: ['400', '600', '800'] });